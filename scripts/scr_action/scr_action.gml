/**
 * @desc Parent constructor for the character actions.
 * The children of this constructor should have an act, a charge
 * and a release method in which the action code itself is written.
 * @param {Id.Instance} _actor the character that has this action
 */
function Action(_actor) constructor {
	
	actor = _actor;
	
}
